# 本文件定义仓库中「可再生、不入库」的内容
# 原则见 AGENTS.md §5：派生产物与源文件严格分野

# --- Python 运行时产物 ---
__pycache__/
*.py[cod]
*.egg-info/
build/
.venv/
venv/

# --- 工具缓存 ---
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- 构建产物（可随时删除并从源文件重建）---
dist/

# --- 本地内容目录（默认内容根）---
# 文章是作者的内容，不属于工具仓库的交付物，因此不入库
# （AGENTS.md §5：源文件与派生产物分野；文章另有同步与备份渠道）。
#
# 默认内容根取 <repo>/content/，让工具与内容都在本地可用；
# 但它**不在版本控制里**——`git status` 不会因为写了文章而变脏。
#
# 若文章放在别处（例如坚果云 vault），用 `inloop --content <路径>`、
# 环境变量 INLOOP_CONTENT 或 config/site.yaml 的 content.root 指定即可，
# 这个目录留空。
content/

# --- 编辑器与系统 ---
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# --- 临时文件 ---
*.tmp
*.bak
*~

# --- 工作区内的临时目录（安装依赖等操作的临时落地处，见 AGENTS.md §2.1）---
.tmp/
.tmp*/

# 防护：`tempfile.mkdtemp()` 在 TEMP 被指向相对路径时会在仓库根留下 tmpXXXX 目录。
# 这类目录绝不应入库，加一条保险以免再次误提交。
tmp??????/
tmp*/

# pytest 的临时根目录：同样可能因 TEMP 为相对路径而落到仓库根
pytest-of-*/
.pytest_tmp/

# --- 独立审核 Agent 的临时脚本 ---
# 审核者在每次审核时会自建验证脚本（前缀 _audit_），它们是用完即弃的，
# 不属于交付物。审核结束后由审核者或主 Agent 删除；这里先挡住避免误提交。
scripts/utils/_audit_*.py
