"""素材处理流水线：扫描、校验、复制图片。

职责边界（AGENTS.md §7）：

- **负责**：找出文章引用的图片、校验格式与体积、复制到产物目录、给出警告。
- **不负责**：决定正文 HTML 里的图片路径写法（由渲染层与发布层约定后统一改写）。

设计约束（AGENTS.md §11 第 2 条）：图片必须以独立文件形式进入产物，
并输出结构化清单，供将来上传到平台后回填路径。
"""
