# AGENTS.md

本文件是仓库根目录下的**长期行为准则**，约束在本仓库内工作的人与 AI Agent。
它只回答一个问题：**在这里应该怎么做事。**

- **本文件不描述需求。** 需求、目录结构、数据字段、技术选型、阶段划分、验收标准、命令清单，一律以仓库根目录的任务书为准（`*任务书*.md`）。
- **本文件不记录当前进度。** 做到哪一步、下一步做什么，以轮次汇报和任务书为准，不写进这里。
- 具体任务与本文冲突时，**以任务书为准**，并立即向人报告冲突点与取舍理由。

---

## 1. 总则

1. **一次只做一轮。** 开工前先把本轮任务清单列给人看；本轮做完、汇报、**停下等人确认**，再开始下一轮。
2. **范围纪律。** 只做任务书在本轮要求的范围。范围外的内容，最多留接口或占位，不实现。
3. **不谎报。** 没有真正执行成功的步骤，不得描述为已完成；无法验证的一律标为「未验证」并说明原因。
4. **唯一事实源。** 内容源文件是唯一事实源，任何脚本不得改写其正文；构建产物是可再生的派生产物，永远不得反向影响源文件。
5. **先可运行，再完整。** 优先让核心链路端到端跑通，再补外围功能。宁可少而可靠，不要多而半成品。
6. **不做未经确认的结构性变更。** 冻结的约定、依赖清单、目录布局、删除已有内容，都必须先问人。

---

## 2. 动手之前

- **先读任务书。** 需求只从任务书取，不从记忆、习惯或猜测里取。
- **先确认环境可用。** 至少确认解释器/运行时版本、所需命令行工具、以及能否在工作目录内执行命令。
- **环境不可用时停下报告**，不得凭推测继续写代码，也不得用"应该可以"替代验证。
- **先看现状再动手。** 修改前先读相关文件与最近的改动，不凭印象改代码。
- **环境事实要沉淀。** 踩过的环境坑（工具路径、权限、沙箱限制）要写进本文件，不靠记忆传递。

### 2.1 本机环境事实（Windows / `E:\InLoopHub`）

- `git` 不在 shell 的 PATH 里，完整路径为 `C:\Program Files\Git\cmd\git.exe`，调用时用完整路径。
- Python 为 `C:\Python313\python.exe`（3.13），`python` 可直接使用。
- **沙箱能力边界**：在受限的工作区写入策略下，MSYS2 系程序（`sh.exe`、`git` 的联网子进程、credential manager）会因无法创建命名对象而崩溃（`NtCreateDirectoryObject ... 0xC0000022`）。
  - 结论：**本地 git 操作（`add` / `commit` / `log` / `status`）在受限策略下可用；`push`、`fetch`、`clone` 需要不受限的执行策略**（已实测：受限时 exit 128，不受限时 exit 0）。
  - 若发现当前策略下无法完成推送，**如实报告并说明如何调整策略**，**不得**静默降级为"跳过推送"并当作已完成。
- **`pip` 在网络可用时即可正常工作**（含联网下载，已在受限策略下实测 exit 0）；但 pip 无法清理系统临时目录，会在 `%TEMP%` 留下残留。
  - 因此安装依赖时**把临时目录指向工作区内**（如设置 `TMPDIR`/`TEMP` 到工作区的临时子目录），用完即清，避免污染系统目录（§5「不留垃圾」）。
- 工作目录 `E:\InLoopHub` 的属主与显式 ACE 已配置（`ZERO\zzr:(OI)(CI)(F)`），默认沙箱在工作区内可正常读写与执行。

---

## 3. 验证与证据

- **每条"完成"都必须能追溯到一次真实执行**，并给出证据（命令 + 关键输出）。
- 汇报结论时使用三态之一：**通过 / 未通过 / 未验证**。禁止"应该没问题""大致通过""理论上可以"。
- 失败就贴**原始错误信息与退出码**，不要只写结论；在原始错误之上再给人话解释。
- **不许为了通过而削弱验证**：不改测试断言、不跳过用例、不放宽校验规则来让结果变绿。
- 修复问题时**先定位根因**，不叠加临时绕过；无法定位时如实说明当前假设与不确定性。

### 3.1 终端的渲染器不是唯一的渲染器

本项目的产物最终由**第三方编辑器**呈现，而不是由本程序或浏览器呈现。两者不等价：

| 差异点 | 事实 | 后果 |
|---|---|---|
| 不是 Markdown 渲染器 | 微信编辑器只认粘贴进来的 HTML，**不解析 Markdown 语法** | 把 `![图](网址)` 或 `**加粗**` 当普通文字显示 |
| 不抓外链 | 不抓取 `https://` 图片，也不抓本地 `file:` 路径 | 图片显示为一段文字或裂图，**只能手动上传** |
| 重写样式 | 粘贴时会重排、合并、丢弃部分声明，且**不保留 CSS 继承** | 依赖父级继承的样式（如 `white-space`）会失效 |
| 折叠空白 | 会丢掉"元素之间"与"元素内部连续"的空格 | 代码块缩进与词间空格消失，示例代码变成语法错误 |

**由此产生的硬性要求：**

1. **凡改动可能影响微信端呈现的，必须标为「未验证」，并明确写出需要人在微信里确认什么。**
   浏览器截图、预览页正常，都**不能**作为该改动通过的证据。
2. **不依赖 CSS 继承。** 需要生效的样式写到每个元素自身；这是"重写样式不保留继承"的直接推论。
3. **空白即语义的地方要显式保护。** 代码块的空格、缩进必须逐字符核对，
   而不是靠肉眼"看着像"；见 `scripts/utils/verify_code_blocks.py`。
4. **不要把"产物看起来对"当成"内容对"。** 内容正确性要用**逐字符比对**验证：
   产物中的代码块应当与源 Markdown 中的代码块完全一致。

**判定标准**：一次改动如果只在本机渲染器下验证过，就不能声称它在微信端可用；
只能声称"本机渲染通过，微信端未验证"，并给出需要人确认的具体点。

### 3.2 遍历可变更容器前先固化快照

同一个坑已踩过两次，因此单列一条：

```python
for child in fragment.children:   # ✗ children 是惰性迭代器
    code.append(child)            #   append 会把节点从 fragment 摘除，
                                  #   边遍历边摘除导致迭代器错位——隔一个取一个
for child in list(fragment.children):   # ✓ 先固化
    code.append(child)
```

这类错误的表现极其隐蔽：不会报错，只是"**一部分元素莫名消失**"，
而且在简单输入上看不出来（本例中恰好丢的都是空白节点）。
在解析树、DOM、任何"取出元素同时会改变其归属"的场景里，都要先 `list(...)`。


---

## 4. 工程规范

- **编码 UTF-8（无 BOM），换行 LF。** 文本文件统一按此写入。
- **路径一律用语言自带的路径类型处理**（如 `pathlib.Path`），禁止手工拼接路径字符串；禁止写死绝对路径。
- **公开函数写完整类型注解。** 边界处做输入校验，失败给出可定位的错误。
- **依赖集中声明**（项目配置文件），不在代码里临时安装或下载；新增第三方依赖必须先问人。
- **配置与常量集中管理并显式读取**，不在代码里散落字面量。
- **输出保持确定性**：同一输入重复执行，产物应一致。不写时间戳、随机数、机器相关路径到产物里，除非任务书要求；确需时间或随机行为时，必须提供可复现的固定入口。
- **不静默失败。** 捕获异常要有理由，要么处理要么上抛；禁止用空 `except` 吞掉错误。
- **错误信息要求**：说清「哪里错了 + 为什么 + 怎么改」，并给出可定位的位置（文件与行号），不写 `invalid input` 这类无信息量的提示。

---

## 5. 工作区整洁与文件归位

**目标：任何人打开仓库，不看文档也能看懂每个目录是干什么的、每个文件为什么在这里。**

- **尊重既有结构。** 目录布局以任务书的规定为准。已有目录能放的内容，不得另起新目录；不新增顶层目录、不把同类文件散落到多处。
- **一个文件只有一个明确的归宿。** 归类看用途，不看方便：配置进配置目录，脚本进脚本目录，测试用例与测试数据各自归位，模板进模板目录，样式进样式目录，文档进文档目录。
- **派生产物与源文件严格分野。** 构建、缓存、临时产物只允许出现在指定的输出目录内；这类产物视为可再生，不提交。
- **不留垃圾。** 调试脚本、临时文件、一次性导出、被替换掉的旧文件，要么删掉，要么放进约定的临时位置并在提交前清理。禁止在仓库里留 `tmp`、`test2`、`新建文件夹`、`xxx.bak`、`xxx_old` 这类无信息量的东西。
- **不堆大文件。** 避免把多个不相关的职责塞进同一个文件；一个文件明显过长或职责混杂时，说明该拆。
- **产物分类存放**，不把不同类别、不同作用域的输出混在一层目录里。
- **目录可自解释。** 新建目录或往里放入非自明用途的文件时，在该目录放一个简短的 README 说明用途与存放规则。
- **提交前检查落盘位置。** 提交前确认本轮改动没有越出既定结构；如果现有结构确实装不下新内容，按 §9 第 6 条先问人方案，**不得**用"先放这里、以后再整理"糊过去。

---

## 6. 语言与命名

- 面向人的内容（CLI 提示、报错、README、文档、提交信息）**用中文**。
- 代码标识符、文件名、路径、命令名**用英文**；避免中文标识符。
- 中文字符串的长度按**字符（码点）**计算，不按字节数判断长短。

---

## 7. 版本控制

**常规流程：每轮代码改动完成后，先自检，再提交，再推送。**

- **提交时机**：本轮功能完成、且项目约定的检查（静态检查、测试等）通过后提交。**检查未通过不得提交**，先修完再提。
- **提交粒度**：一个提交 = 一个完整可读的改动单元。不按"改了一个文件"拆提交，也不把互不相关的改动塞进同一个提交。
- **提交内容**：只提交本轮有意改动的文件。提交前核对差异，确认没有夹带临时文件、调试残留、密钥、令牌、大体积二进制与可再生的构建产物。
- **提交信息**：用中文，简要写清**本轮目标与主要改进**（做了什么、为什么），而不是罗列改了哪些文件。
- **推送**：提交后推送到远端对应分支，并如实报告推送结果。
- **推送失败必须如实报告**（无远端、无网络、无凭据、无权限等），说明原因并给出可执行的下一步；**不得**声称已推送，**不得**为了推送成功而自行修改远端地址、跳过校验或改用危险参数。本地提交保留，不撤销。
- **不执行**：强制推送、改写历史（`reset --hard`、`rebase` 已推送的提交）、删除远端分支。
- 若远端或分支约定尚未确定，按 §9 先问人，**不得**自行创建远端仓库或猜测地址。

---

## 8. 汇报格式

每轮结束按以下结构汇报，缺项要写明为什么缺：

1. **本轮做了什么**：改动文件清单（含用途一句话）。
2. **怎么验证的**：逐条列「结论 + 命令/证据」，用 通过 / 未通过 / 未验证。
3. **已知问题与不确定点**：包括未覆盖的边界情况与临时取舍。
4. **建议的下一步**：可选方案与推荐项，标明理由。
5. **需要人确认的问题**：只列真正影响方向的，不把小事推给人。

---

## 9. 什么时候必须停下来问人

出现以下任一情况，立即停下，说明情况与可选方案，等人决定：

1. 需要修改任务书已确定的约定（结构、字段、接口、选型、样式基调）。
2. 需要新增或替换第三方依赖。
3. 需要删除、重写或迁移已有内容/数据。
4. 权限、沙箱、网络等环境限制导致无法验证。
5. 发现任务书表述矛盾、缺失或与可实现性冲突。
6. 要在两种会显著影响后续维护成本的方案之间做选择。

---

## 10. 禁止事项

- 编造未执行的结果、日志、测试输出或文件内容。
- 超出本轮范围"顺手"实现功能，或引入任务书明确排除的技术。
- 把任务书要求的具体值（字段名、颜色、字号、阈值等）在代码里重复硬编码，而非集中配置读取。
- 静默吞掉错误、用注释掉代码代替删除、留大段无信息的 `TODO`。
- 未经确认执行破坏性操作（删除、覆盖、批量重命名、改写历史）。

---

## 11. 项目终局与架构约束

**终局目标**：人只管把 Markdown 放进仓库，由本项目的程序完成到微信公众号的发布，取代手工复制粘贴。

第一阶段（半自动）只是这条路上的中间态：产物先由人过一遍再贴进后台，是因为格式、图片、封面、链接还需要人工兜底。**任何"省掉人工"都以保住这条终局路径为前提**，不允许为了眼前的省事把未来的自动化堵死。由此产生三条硬约束：

1. **渲染产物必须是微信接口可直接消费的形态。** 正文容器内不存在 `<script>`、外链 CSS、`iframe`，样式全部内联；不得引入任何只在浏览器里成立、进编辑器就失效的机制。
2. **图片必须是可上传的实体文件，路径可重写。** 构建产物中的图片以独立文件形式存在并带结构化清单（相对路径 + 尺寸/字节数），供后续逐张上传换取平台地址后回填正文。**禁止**把图片内联成 data URI 或烧进 HTML 导致无法回填。
3. **平台适配层与内容源彻底解耦。** 发布接口的抽象（构建 / 发布 / 更新）必须与文章模型、渲染器、CLI 参数相互独立。将来接入真实发布能力时，只允许新增适配层实现，**不允许**改动文章正文结构、front matter 字段或既有命令语义。

**判定标准**：任何一次改动，如果会让"未来无人值守发布"变难（例如把信息只留在 HTML 里、把平台细节渗进内容源、让渲染器依赖具体平台账号），就是错误方向，按 §9 停下问人。

